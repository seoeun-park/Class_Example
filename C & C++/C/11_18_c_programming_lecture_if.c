#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
int main() {

	// 변수 선언

	// 사용자가 입력한 값을 저장할 변수
	int x, y;

	// 사용자가 입력한 값 중에서 작은 값을 구해서 저장할 변수
	int max;
	int min;

	// 사용자에게 입력을 받기 위해 출력

	printf("Enter two integers: ");

	// scanf 함수를 통해 표준 입력(키보드)에서 입력
	// → scanf 함수를 이용해 2개 이상의 값을 읽어올 때 값들은
	//	공백문자(white-space: 빈칸, 개행문자, 탭)으로 구분된다

	scanf("%d %d", &x, &y);

	//조건 연산자를 이용해 사용자가 입력한 값 중에서 작은 값을 계산
	min = (x < y) ? x : y;
	max = (x > y) ? x : y;

	//출력
	printf("\nmin = %d\n", min);
	printf("max = %d", max);


	return 0;
}
*/

//------------------------------------------------------------------
/*
int main () {

	// if 조건문
	int num;

	// 사용자가 값을 입력할 수 있도록 출력
	printf("Enter the integers: ");

	// scanf 함수를 이용해 사용자로부터 값을 입력
	scanf("%d", &num);

	if (num > 40)
		printf("the integer is over 40.\n");

	if (num > 10 && num < 40)
		printf("the integer is between 10 and 40.\n");

	return 0;
}
*/

